---
import Card from "./Card.astro";
import Typography from "./Typography.astro";
---

<Card flexOptions="flex flex-col gap-2 justify-center items-center">
  <Typography id="clock" as="h2" variant="title" />
  <Typography as="p" variant="body">🇨🇴 Bogotá, Colombia</Typography>
</Card>

<script>
  function updateClock() {
    const bogotaTime = new Intl.DateTimeFormat("es-CO", {
      timeZone: "America/Bogota",
      hour: "2-digit",
      minute: "2-digit",
      second: "2-digit",
      hour12: false,
    }).format(new Date());

    const clockElement = document.getElementById("clock");
    if (clockElement) clockElement.textContent = `${bogotaTime} GMT-5`;
  }
  updateClock();
  setInterval(updateClock, 1000);

</script>
